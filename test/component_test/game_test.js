const { expect } = require('chai');
const { Game } = require('../bin/game.js');


Describe('', () => {
    Describe('Results of the Game', () => {
        xit('Displays when Player Loses', () => {
            expect(());
        });
        xit('Displays when Player Wins', () => {
            expect(());
        });
    });
});